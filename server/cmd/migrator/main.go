package main

import "fmt"

func main() {
	// var newMigrationCommand = "migrate create -ext sql -dir ./schema -seq init"
	// var migrationUpCommand = "migrate -path ./migrations -database 'postgres://postgres:paveq123@localhost:5454/postgres?sslmode=disable' up"

	command := "up"

	fmt.Println(command)
}
